run = ["npm", "run", "dev"]

entrypoint = "index.html"

[env]
NODE_ENV = "development"
PORT = "5173"

[install]
cmd = "npm install"

language = "nodejs"  # ✅ CORRECT way to specify language

[nix]
channel = "stable-24_11"

[gitHubImport]
requiredFiles = [".replit", "replit.nix", "package.json", "vite.config.js"]
